spring.application.name=meteo

# Porta del server
server.port=8080

# Dettagli dei log per Spring Web
logging.level.org.springframework.web=DEBUG
logging.level.com.meteo=DEBUG

# Chiave API di Open-Meteo (se necessaria)
# Se non è necessaria, puoi omettere questa configurazione
open-meteo.api.key=${OPEN_METEO_API_KEY}

# Timeout per le richieste HTTP a Open-Meteo
spring.web.client.timeout=5000
